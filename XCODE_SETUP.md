# Xcode Project Setup Instructions

## ✅ Что уже создано

Я создал следующую структуру файлов для iOS приложения:

```
workout tracker app/
├── Models/
│   ├── User.swift ✅
│   ├── Workout.swift ✅
│   ├── Exercise.swift ✅
│   └── WorkoutSet.swift ✅
├── Services/
│   └── SupabaseService.swift ✅
├── Utilities/
│   └── Config.swift ✅
├── Views/ (пустая структура)
│   ├── Auth/
│   ├── Main/
│   ├── Recording/
│   └── Components/
└── ViewModels/ (пустая структура)
```

---

## 📦 Шаг 1: Добавить Supabase Swift SDK

### Через Xcode (рекомендуется):

1. Откройте проект `workout tracker app.xcodeproj` в Xcode
2. File → Add Package Dependencies...
3. В поисковой строке введите:
   ```
   https://github.com/supabase/supabase-swift
   ```
4. Version: **2.0.0** или выше (выберите "Up to Next Major Version")
5. Нажмите **Add Package**
6. Выберите следующие продукты для добавления:
   - ✅ **Supabase**
   - ✅ **Auth**
   - ✅ **PostgREST**
   - ✅ **Storage** (опционально)
   - ✅ **Realtime** (опционально)
7. Нажмите **Add Package**

Xcode автоматически скачает и интегрирует зависимости.

---

## 📂 Шаг 2: Добавить файлы в проект

Все файлы уже созданы в файловой системе, но нужно добавить их в Xcode проект:

### Метод 1: Перетаскивание (Drag & Drop)

1. В Finder откройте папку `workout tracker app/`
2. В Xcode откройте Project Navigator (⌘1)
3. Перетащите следующие папки в Xcode:
   - **Models/** → перетащите всю папку
   - **Services/** → перетащите всю папку
   - **Utilities/** → перетащите всю папку
   - **Views/** → перетащите всю папку (с подпапками)
   - **ViewModels/** → перетащите всю папку

4. В диалоге выберите:
   - ✅ Copy items if needed
   - ✅ Create groups
   - ✅ Add to targets: workout tracker app

### Метод 2: Через меню Xcode

1. Правый клик на группу "workout tracker app" в Project Navigator
2. **Add Files to "workout tracker app"...**
3. Выберите папки: Models, Services, Utilities, Views, ViewModels
4. Убедитесь что выбраны все опции как выше

---

## ⚙️ Шаг 3: Проверка компиляции

После добавления файлов:

1. Нажмите **⌘B** (Build)
2. Вы увидите ошибки - это нормально! Потому что:
   - Supabase SDK может еще не быть полностью скачан
   - Некоторые файлы ссылаются на Views которые еще не созданы

3. Дождитесь завершения загрузки пакетов (см. статус в верхней части Xcode)

4. Попробуйте билд снова

---

## 🔑 Шаг 4: Защита секретов

Файл **Config.swift** содержит API ключи. Убедитесь что:

1. Файл [.gitignore](.gitignore) уже создан ✅
2. Config.swift добавлен в .gitignore ✅
3. НЕ коммитьте Config.swift в git!

---

## 🧪 Шаг 5: Тестирование подключения к Supabase

После успешной компиляции, протестируем подключение.

Откройте **ContentView.swift** и замените содержимое на:

```swift
import SwiftUI

struct ContentView: View {
    @StateObject private var supabaseService = SupabaseService.shared

    var body: some View {
        VStack(spacing: 20) {
            if supabaseService.isAuthenticated {
                Text("✅ Подключено к Supabase!")
                    .font(.title)
                    .foregroundColor(.green)

                if let user = supabaseService.currentUser {
                    Text("Email: \(user.email)")
                        .font(.caption)
                }

                Button("Sign Out") {
                    Task {
                        try? await supabaseService.signOut()
                    }
                }
                .buttonStyle(.borderedProminent)
            } else {
                Text("Workout Tracker")
                    .font(.largeTitle)
                    .bold()

                Text("Пока не авторизован")
                    .font(.caption)
                    .foregroundColor(.gray)
            }
        }
        .padding()
        .task {
            await supabaseService.checkCurrentSession()
        }
    }
}

#Preview {
    ContentView()
}
```

Запустите приложение (⌘R) и вы должны увидеть текст "Пока не авторизован".

Это значит подключение к Supabase работает! 🎉

---

## 🎯 Следующие шаги

После успешной настройки Xcode проекта:

1. ✅ Создать AuthViewModel и экраны входа/регистрации
2. ✅ Создать WorkoutViewModel
3. ✅ Создать UI для списка тренировок
4. ✅ Создать VoiceRecordingService для голосового ввода
5. ✅ Интегрировать AI для обработки речи

---

## 🆘 Troubleshooting

### Ошибка: "No such module 'Supabase'"

**Решение:**
1. Убедитесь что пакет полностью скачался (см. статус вверху Xcode)
2. Попробуйте: File → Packages → Reset Package Caches
3. Clean Build Folder (⇧⌘K) и Build снова (⌘B)

### Ошибка: "Type 'SupabaseClient' has no member"

**Решение:**
- Возможно версия SDK не совпадает. Убедитесь что используете версию 2.0.0+
- Проверьте документацию: https://supabase.com/docs/reference/swift

### Файлы не видны в Xcode

**Решение:**
- Убедитесь что файлы добавлены через Xcode (не напрямую в Finder)
- Проверьте что Target Membership включен для файлов

---

## 📚 Полезные ресурсы

- [Supabase Swift SDK](https://github.com/supabase/supabase-swift)
- [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui)
- [PLAN.md](PLAN.md) - полный план разработки
- [supabase/SCHEMA.md](supabase/SCHEMA.md) - структура БД

---

**Готово к следующему шагу?** Напишите мне и я продолжу с созданием UI и авторизации!
