# План разработки: Workout Tracker с голосовым вводом

## Обзор проекта

**Название**: Workout Tracker App
**Платформа**: iOS (SwiftUI)
**Бэкенд**: Supabase
**Референс**: [Dropset](https://www.getdropset.app/)

### Основная идея
Трекер тренировок с голосовым вводом данных. Пользователь говорит что он сделал (упражнение, подходы, повторения, сложность, RIR), и автоматически создается карточка тренировки.

---

## MVP Функционал (Первая версия)

### 1. Авторизация
- Email/Password через Supabase Auth
- Экран входа/регистрации
- Автоматический вход при повторном запуске
- Выход из аккаунта

### 2. Голосовой ввод тренировки
- Кнопка записи голоса (большая, удобная для зала)
- Транскрипция аудио → текст
- AI обработка текста (GPT/Claude API) для извлечения:
  - Название упражнения
  - Количество подходов
  - Повторения в каждом подходе
  - RPE (Rate of Perceived Exertion, 1-10)
  - RIR (Reps in Reserve - повторения в запасе)
- Поддержка русского и английского языка
- Показ распознанного текста для проверки
- Подтверждение перед сохранением

### 3. Просмотр истории тренировок
- Список всех тренировок (сортировка по дате)
- Карточки тренировок с основной информацией
- Детальный просмотр конкретной тренировки
- Удаление тренировок

### 4. Редактирование тренировок
- Ручное редактирование всех полей
- Добавление упражнений вручную
- Редактирование подходов и повторений
- Изменение RPE и RIR

---

## Будущие фичи (после MVP)

### Фаза 2
- **Суперсеты**: Группировка упражнений
- **Таймеры отдыха**: Автоматический таймер между подходами
- **Шаблоны тренировок**: Сохранение программ тренировок

### Фаза 3
- **История и аналитика**: Графики прогресса по упражнениям
- **Статистика**: Объем нагрузки, частота тренировок

### Фаза 4
- **AI Чат-ассистент**: Анализ данных тренировок и рекомендации

---

## Архитектура приложения

### Frontend (iOS - SwiftUI)

#### Структура проекта
```
workout tracker app/
├── Models/
│   ├── Workout.swift
│   ├── Exercise.swift
│   ├── WorkoutSet.swift
│   └── User.swift
├── Views/
│   ├── Auth/
│   │   ├── LoginView.swift
│   │   └── RegisterView.swift
│   ├── Main/
│   │   ├── MainTabView.swift
│   │   ├── WorkoutListView.swift
│   │   └── WorkoutDetailView.swift
│   ├── Recording/
│   │   ├── VoiceRecordingView.swift
│   │   └── WorkoutConfirmationView.swift
│   └── Components/
│       ├── WorkoutCard.swift
│       ├── ExerciseRow.swift
│       └── RecordButton.swift
├── ViewModels/
│   ├── AuthViewModel.swift
│   ├── WorkoutViewModel.swift
│   └── VoiceRecordingViewModel.swift
├── Services/
│   ├── SupabaseService.swift
│   ├── VoiceRecognitionService.swift
│   ├── AIProcessingService.swift
│   └── AudioRecorderService.swift
├── Utilities/
│   ├── Constants.swift
│   └── Extensions.swift
└── Resources/
    └── Assets.xcassets
```

#### Ключевые технологии
- **UI**: SwiftUI
- **Архитектура**: MVVM
- **Голосовой ввод**: AVFoundation для записи аудио
- **State Management**: @StateObject, @ObservedObject, @EnvironmentObject

### Backend (Supabase)

#### Структура базы данных

**Таблица: users**
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Таблица: workouts**
```sql
CREATE TABLE workouts (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  date TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  notes TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Таблица: exercises**
```sql
CREATE TABLE exercises (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  workout_id UUID REFERENCES workouts(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  exercise_order INTEGER NOT NULL,
  notes TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Таблица: workout_sets**
```sql
CREATE TABLE workout_sets (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  exercise_id UUID REFERENCES exercises(id) ON DELETE CASCADE,
  set_number INTEGER NOT NULL,
  reps INTEGER NOT NULL,
  weight NUMERIC(5,2),
  rpe INTEGER CHECK (rpe >= 1 AND rpe <= 10),
  rir INTEGER CHECK (rir >= 0),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);
```

**Индексы**
```sql
CREATE INDEX idx_workouts_user_id ON workouts(user_id);
CREATE INDEX idx_workouts_date ON workouts(date DESC);
CREATE INDEX idx_exercises_workout_id ON exercises(workout_id);
CREATE INDEX idx_sets_exercise_id ON workout_sets(exercise_id);
```

**Row Level Security (RLS)**
```sql
-- Пользователи видят только свои данные
ALTER TABLE workouts ENABLE ROW LEVEL SECURITY;
ALTER TABLE exercises ENABLE ROW LEVEL SECURITY;
ALTER TABLE workout_sets ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users can view own workouts" ON workouts
  FOR SELECT USING (auth.uid() = user_id);

CREATE POLICY "Users can insert own workouts" ON workouts
  FOR INSERT WITH CHECK (auth.uid() = user_id);

CREATE POLICY "Users can update own workouts" ON workouts
  FOR UPDATE USING (auth.uid() = user_id);

CREATE POLICY "Users can delete own workouts" ON workouts
  FOR DELETE USING (auth.uid() = user_id);

-- Аналогичные политики для exercises и workout_sets
CREATE POLICY "Users can view own exercises" ON exercises
  FOR SELECT USING (
    EXISTS (
      SELECT 1 FROM workouts
      WHERE workouts.id = exercises.workout_id
      AND workouts.user_id = auth.uid()
    )
  );

-- И так далее для всех операций...
```

---

## Обработка голосового ввода

### Workflow

1. **Запись аудио** (iOS AVFoundation)
   - Пользователь зажимает кнопку записи
   - Аудио записывается в формате M4A
   - При отпускании - запись завершается

2. **Транскрипция** (Whisper API или iOS Speech Recognition)
   - Аудио отправляется на обработку
   - Получаем текст транскрипции
   - Показываем пользователю для проверки

3. **AI парсинг** (Claude/GPT API)
   - Отправляем текст в AI с промптом
   - AI извлекает структурированные данные
   - Возвращает JSON с упражнениями

4. **Подтверждение**
   - Показываем пользователю что распознали
   - Даем возможность исправить
   - Сохраняем в Supabase

### Пример промпта для AI

```
Проанализируй следующий текст о тренировке и извлеки структурированную информацию.
Верни JSON в формате:

{
  "exercises": [
    {
      "name": "название упражнения",
      "sets": [
        {
          "reps": количество повторений,
          "rpe": сложность от 1 до 10 (если указана),
          "rir": повторения в запасе (если указаны)
        }
      ]
    }
  ]
}

Текст: "{распознанный_текст}"

Правила:
- Названия упражнений на русском или английском
- Если RPE или RIR не указаны, ставь null
- Если упражнение одно, но несколько подходов - группируй их
```

### Примеры голосового ввода

**Русский:**
> "Жим лежа 4 подхода: 10, 8, 8, 6 повторений, RPE 8, оставалось 2 повтора в запасе"

**Английский:**
> "Bench press 4 sets: 10, 8, 8, 6 reps, RPE 8, 2 reps in reserve"

**Несколько упражнений:**
> "Приседания 3 по 10, становая тяга 4 подхода: 8, 6, 5, 5, жим ногами 3 по 12"

---

## Дизайн UI/UX

### Стиль
- **Минималистичный** (как Dropset)
- **Крупные элементы** (удобно в зале)
- **Контрастные цвета** (видно при плохом освещении)
- **Быстрый доступ** к основным функциям

### Цветовая схема
```swift
// Primary colors
- Accent: RGB(255, 69, 58) // Яркий красный для кнопок
- Background: RGB(0, 0, 0) // Черный фон
- Card Background: RGB(28, 28, 30) // Темно-серые карточки
- Text Primary: RGB(255, 255, 255) // Белый текст
- Text Secondary: RGB(142, 142, 147) // Серый текст

// Semantic colors
- Success: RGB(48, 209, 88)
- Warning: RGB(255, 214, 10)
- Error: RGB(255, 69, 58)
```

### Основные экраны

#### 1. Экран записи (главный)
```
┌─────────────────────────┐
│   🗓️ Сегодня            │
│   25 января 2026        │
│                         │
│                         │
│        ⬤ REC            │ <- Большая кнопка записи
│                         │
│   Нажми для записи      │
│                         │
│                         │
│  ─────────────────────  │
│                         │
│   📊 История тренировок │ <- Кнопка к истории
└─────────────────────────┘
```

#### 2. Список тренировок
```
┌─────────────────────────┐
│  ← История              │
│                         │
│  ┌───────────────────┐  │
│  │ 25.01.2026        │  │
│  │ 5 упражнений      │  │
│  │ 45 мин            │  │
│  └───────────────────┘  │
│                         │
│  ┌───────────────────┐  │
│  │ 23.01.2026        │  │
│  │ 4 упражнения      │  │
│  │ 38 мин            │  │
│  └───────────────────┘  │
└─────────────────────────┘
```

#### 3. Детали тренировки
```
┌─────────────────────────┐
│  ← Жим лежа          ✏️  │
│                         │
│  Подход 1: 10 повт     │
│  Подход 2: 8 повт      │
│  Подход 3: 8 повт      │
│  Подход 4: 6 повт      │
│                         │
│  RPE: 8/10             │
│  RIR: 2                │
│                         │
│  🗑️ Удалить упражнение  │
└─────────────────────────┘
```

---

## Пошаговый план разработки

### Этап 1: Подготовка (1-2 дня)
- [ ] Настроить Xcode проект
- [ ] Установить зависимости (Supabase Swift SDK)
- [ ] Настроить Supabase проект
  - [ ] Создать таблицы в БД
  - [ ] Настроить RLS политики
  - [ ] Включить Authentication
- [ ] Получить API ключи (Supabase, AI API)

### Этап 2: Модели данных (1 день)
- [ ] Создать модель `User`
- [ ] Создать модель `Workout`
- [ ] Создать модель `Exercise`
- [ ] Создать модель `WorkoutSet`
- [ ] Добавить Codable conformance для работы с JSON

### Этап 3: Supabase интеграция (2 дня)
- [ ] Настроить `SupabaseService`
- [ ] Реализовать методы авторизации
  - [ ] Sign up
  - [ ] Sign in
  - [ ] Sign out
  - [ ] Восстановление сессии
- [ ] Реализовать CRUD для тренировок
  - [ ] Создание тренировки
  - [ ] Получение списка тренировок
  - [ ] Получение деталей тренировки
  - [ ] Обновление тренировки
  - [ ] Удаление тренировки

### Этап 4: Авторизация UI (1-2 дня)
- [ ] Создать `LoginView`
- [ ] Создать `RegisterView`
- [ ] Создать `AuthViewModel`
- [ ] Настроить навигацию (авторизован/не авторизован)
- [ ] Добавить валидацию форм
- [ ] Обработка ошибок

### Этап 5: Голосовой ввод - запись (2 дня)
- [ ] Создать `AudioRecorderService`
  - [ ] Запрос разрешения на микрофон
  - [ ] Запись аудио (AVAudioRecorder)
  - [ ] Сохранение файла
- [ ] Создать `VoiceRecordingView`
  - [ ] Кнопка записи (hold to record)
  - [ ] Анимация при записи
  - [ ] Индикатор уровня звука
- [ ] Создать `VoiceRecordingViewModel`

### Этап 6: Голосовой ввод - распознавание (2-3 дня)
- [ ] Создать `VoiceRecognitionService`
  - [ ] Интеграция с Whisper API (или iOS Speech)
  - [ ] Обработка аудио файла
  - [ ] Получение транскрипции
- [ ] Создать `AIProcessingService`
  - [ ] Интеграция с Claude/GPT API
  - [ ] Промпт для парсинга тренировок
  - [ ] Парсинг JSON ответа
- [ ] Обработка ошибок и edge cases
- [ ] Поддержка русского и английского

### Этап 7: Подтверждение и сохранение (1-2 дня)
- [ ] Создать `WorkoutConfirmationView`
  - [ ] Показ распознанного текста
  - [ ] Показ извлеченных данных
  - [ ] Кнопки "Сохранить" / "Отменить" / "Исправить"
- [ ] Интеграция с Supabase для сохранения
- [ ] Обновление UI после сохранения

### Этап 8: Просмотр истории (2 дня)
- [ ] Создать `WorkoutListView`
  - [ ] Список тренировок
  - [ ] Pull-to-refresh
  - [ ] Загрузка данных из Supabase
- [ ] Создать `WorkoutCard` компонент
- [ ] Создать `WorkoutViewModel`
- [ ] Добавить пустое состояние (empty state)
- [ ] Добавить loading состояния

### Этап 9: Детальный просмотр тренировки (1-2 дня)
- [ ] Создать `WorkoutDetailView`
  - [ ] Отображение всех упражнений
  - [ ] Отображение подходов для каждого упражнения
  - [ ] Показ RPE, RIR
- [ ] Создать `ExerciseRow` компонент
- [ ] Навигация из списка в детали
- [ ] Кнопка удаления тренировки

### Этап 10: Редактирование тренировок (2-3 дня)
- [ ] Создать `WorkoutEditView`
  - [ ] Формы для редактирования упражнений
  - [ ] Добавление/удаление подходов
  - [ ] Изменение значений
- [ ] Ручное добавление упражнений
- [ ] Валидация данных
- [ ] Сохранение изменений в Supabase

### Этап 11: UI polish и UX (2-3 дня)
- [ ] Применить цветовую схему
- [ ] Добавить анимации и transitions
- [ ] Улучшить feedback (haptics, звуки)
- [ ] Оптимизация для разных размеров экранов
- [ ] Dark mode
- [ ] Accessibility (VoiceOver)

### Этап 12: Тестирование и баг-фиксы (2-3 дня)
- [ ] Тестирование на реальном устройстве
- [ ] Тестирование голосового ввода в реальных условиях
- [ ] Проверка edge cases
- [ ] Исправление багов
- [ ] Оптимизация производительности

### Этап 13: Подготовка к релизу (1-2 дня)
- [ ] Добавить иконку приложения
- [ ] Добавить launch screen
- [ ] Проверка на утечки памяти
- [ ] Финальное тестирование
- [ ] Подготовка для TestFlight (опционально)

**Общее время разработки MVP: ~3-4 недели**

---

## Технические зависимости

### Swift Package Manager (SPM)

```swift
dependencies: [
    .package(url: "https://github.com/supabase/supabase-swift", from: "2.0.0"),
]
```

### Необходимые frameworks
- SwiftUI (UI)
- Combine (reactive programming)
- AVFoundation (audio recording)
- Speech (опционально для iOS speech recognition)

### External APIs
- **Supabase**: База данных, авторизация
- **OpenAI Whisper API**: Транскрипция аудио (опционально)
- **Claude API** или **OpenAI GPT API**: Парсинг текста тренировок

---

## Конфигурация и секреты

### Environment Variables

Создать файл `Config.swift`:

```swift
enum Config {
    // Supabase
    static let supabaseURL = "YOUR_SUPABASE_URL"
    static let supabaseAnonKey = "YOUR_SUPABASE_ANON_KEY"

    // AI API
    static let claudeAPIKey = "YOUR_CLAUDE_API_KEY"
    // или
    static let openAIAPIKey = "YOUR_OPENAI_API_KEY"
}
```

⚠️ **Важно**: Не коммитить в git! Добавить в `.gitignore`

---

## Best Practices

### 1. Безопасность
- Не хранить API ключи в коде
- Использовать RLS в Supabase
- Валидация данных на клиенте И сервере
- HTTPS для всех запросов

### 2. Производительность
- Кэширование данных локально (UserDefaults для простых данных)
- Lazy loading для списков
- Оптимизация изображений
- Минимизация API запросов

### 3. UX в зале
- **Большие кнопки** (минимум 44x44pt)
- **Высокий контраст** (читаемость при ярком свете)
- **Быстрый доступ** (минимум тапов до записи)
- **Offline режим** (сохранение локально, синхронизация позже)

### 4. Обработка ошибок
- Понятные сообщения об ошибках для пользователя
- Логирование для отладки
- Graceful degradation (fallback опции)
- Retry механизм для сетевых запросов

### 5. Тестирование
- Unit тесты для бизнес-логики
- UI тесты для критичных флоу
- Тестирование на разных моделях iPhone
- Тестирование в реальных условиях зала

---

## Возможные проблемы и решения

### Проблема 1: Шум в зале мешает распознаванию
**Решение**:
- Использовать шумоподавление в настройках AVAudioSession
- Инструкция пользователю держать телефон ближе к лицу
- Возможность повторной записи

### Проблема 2: AI неправильно парсит упражнения
**Решение**:
- Улучшение промпта с примерами
- Показ результата парсинга перед сохранением
- Возможность ручной корректировки
- Сбор feedback для улучшения промпта

### Проблема 3: Медленная транскрипция
**Решение**:
- Показывать loading индикатор
- Использовать iOS Speech Recognition для быстрой работы
- Кэшировать результаты

### Проблема 4: Нет интернета в зале
**Решение**:
- Сохранять аудио локально
- Обрабатывать позже когда появится интернет
- Показывать статус синхронизации

---

## Roadmap после MVP

### Версия 1.1 - Суперсеты и таймеры
- Группировка упражнений в суперсеты
- Таймер отдыха между подходами
- Настройка времени отдыха

### Версия 1.2 - Шаблоны
- Сохранение программ тренировок
- Быстрый старт из шаблона
- Редактирование шаблонов

### Версия 1.3 - Аналитика
- Графики прогресса по упражнениям
- Статистика объема
- Tracking PR (personal records)

### Версия 2.0 - AI Assistant
- Чат с анализом тренировок
- Рекомендации по прогрессии
- Персонализированные советы
- Интеграция с данными тренировок

---

## Контрольный список запуска

### Перед началом разработки
- [ ] Создан проект в Supabase
- [ ] Получены все API ключи
- [ ] Настроен Xcode проект
- [ ] Установлены зависимости
- [ ] Создан `.gitignore` для секретов

### Перед тестированием MVP
- [ ] Все функции MVP реализованы
- [ ] Базовое тестирование пройдено
- [ ] UI соответствует дизайну
- [ ] Нет критичных багов

### Перед релизом
- [ ] Финальное тестирование на устройстве
- [ ] Иконка и launch screen
- [ ] Privacy policy и terms of service
- [ ] App Store описание и скриншоты
- [ ] Настроена аналитика (опционально)

---

## Полезные ресурсы

### Документация
- [Supabase Swift Documentation](https://supabase.com/docs/reference/swift)
- [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui)
- [AVFoundation Guide](https://developer.apple.com/av-foundation/)
- [Claude API Documentation](https://docs.anthropic.com/claude/reference)

### Туториалы
- Supabase + SwiftUI Authentication
- AVAudioRecorder Tutorial
- Speech Recognition in iOS
- SwiftUI MVVM Architecture

---

## Заметки

### Что нужно уточнить перед стартом:
1. ✅ Supabase проект URL и ключи
2. ✅ Выбор AI API (Claude vs GPT)
3. ⚠️ Бюджет на API вызовы
4. ⚠️ Нужен ли TestFlight или сразу App Store

### Skills для лучшей разработки:
- iOS/SwiftUI skill для навигации по проекту
- Supabase skill для работы с БД
- AI integration skill для работы с API

---

**Последнее обновление**: 25 января 2026
